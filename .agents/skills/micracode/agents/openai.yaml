interface:
  display_name: "Micracode"
  short_description: "Repo-specific patterns and workflows for micracode"
  default_prompt: "Use the micracode repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true